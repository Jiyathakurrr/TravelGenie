name: TravelGenie CI

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
    branches: [ "main", "develop" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'

    # Backend Setup & Test (Placeholder)
    - name: Install Backend Dependencies
      run: |
        # cd backend
        # npm ci
        echo "Backend install skipped for placeholder"

    - name: Run Backend Tests
      run: |
        # cd backend
        # npm run test
        echo "Backend tests skipped for placeholder"

    # Frontend Setup & Test (Placeholder)
    - name: Install Frontend Dependencies
      run: |
        # cd frontend
        # npm ci
        echo "Frontend install skipped for placeholder"

    - name: Run Frontend Lint & Test
      run: |
        # cd frontend
        # npm run lint
        # npm run test
        echo "Frontend tests skipped for placeholder"
